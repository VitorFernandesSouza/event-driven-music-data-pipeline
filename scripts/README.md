# Scripts

**Status:** Structure only. No exploration, validation, download, or pipeline scripts are implemented.

Local helper scripts will be added as repeatable workflows are introduced. Scripts must not contain credentials or assume a specific personal environment.
