# dbt

The planned dbt flow is:

```text
RAW -> STAGING -> INTERMEDIATE -> MARTS
```

Models, tests, macros, seeds, and snapshots will be defined after public datasets are selected and explored. No final dbt models exist yet.
